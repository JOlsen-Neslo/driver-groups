<% provide(:title, 'Grouped Trips') %>
<h1 class="text-center">Grouped Trips</h1>

<div class="row">
    <div class="col-md-12">
        <%= form_tag('grouped_trips') %>
        <%= select_tag :time_slot, options_for_select(@slots, 1), class: 'form-control' %>
        <button type="submit" class="btn btn-primary mt-2">Submit</button>
    </div>
</div>
